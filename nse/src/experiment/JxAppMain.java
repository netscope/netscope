package experiment;

public class JxAppMain {

	/**
	 * The basic steps to create a test simulator is as the following:
	 * 
	 * JxSimulator ns = new JxSimulator(...);
	 * ns.add( new JxMeshTopology(...) );
	 * ns.add( new JxMeshRule(...) );
	 * ns.add( new JxMeshTrace(...) );
	 * ns.step(1000);
	 *
	 * There're also some pre-defined simulators such as JxDlaSimulator you can use
	 * directly.
	 * 
	 * @param args
	 */
	public static void main(String[] args) {

	}

	
}
